@page
@model Addidas_Outlet.Pages.Sub_Categories.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Sub_Category</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Sub_Category.id" />
            <div class="form-group">
                <label asp-for="Sub_Category.Sub_Categories" class="control-label"></label>
                <input asp-for="Sub_Category.Sub_Categories" class="form-control" />
                <span asp-validation-for="Sub_Category.Sub_Categories" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
