@page
@model Addidas_Outlet.Pages.Sub_Categories.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Sub_Category</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Sub_Category.Sub_Categories)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Sub_Category.Sub_Categories)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Sub_Category.id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
